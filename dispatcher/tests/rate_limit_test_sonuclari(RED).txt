============================= test session starts ==============================
platform linux -- Python 3.10.12, pytest-8.3.4, pluggy-1.6.0 -- /usr/bin/python3
rootdir: /home/duha/yazlab.II-I/dispatcher
collected 2 items                                                              

dispatcher/tests/test_rate_limit.py::TestRateLimiter::test_rate_limit_exceeded_returns_429 FAILED [ 50%]
dispatcher/tests/test_metrics.py::TestMetrics::test_metrics_endpoint_returns_prometheus_format FAILED [100%]

=================================== FAILURES ===================================
_____________ TestRateLimiter.test_rate_limit_exceeded_returns_429 _____________

self = <tests.test_rate_limit.TestRateLimiter object at 0x787ca5bcfc10>
client = <starlette.testclient.TestClient object at 0x787ca5bcd180>

    def test_rate_limit_exceeded_returns_429(self, client):
        """Eşik değeri aşıldığında istek 429 Too Many Requests dönmeli."""
    
        # Saniyede 5 istek limiti olduğunu varsayıyoruz.
        # İlk 5 istek başarılı geçmeli.
        for _ in range(5):
            response = client.get("/health")
            assert response.status_code == 200
    
        # 6. istek eşiği aştığı için rate limiter tarafından bloklanmalı
        # Şu an rate limiter yazılımadığı için bu beklenen şekilde 429 dönmeyecek ve test kızaracak (RED).
        response = client.get("/health")
>       assert response.status_code == 429
E       assert 200 == 429
E        +  where 200 = <Response [200 OK]>.status_code

dispatcher/tests/test_rate_limit.py:26: AssertionError
_________ TestMetrics.test_metrics_endpoint_returns_prometheus_format __________

self = <tests.test_metrics.TestMetrics object at 0x787ca5bcf490>
client = <starlette.testclient.TestClient object at 0x787ca5bc0700>

    def test_metrics_endpoint_returns_prometheus_format(self, client):
        """/metrics endpoint'i çağrıldığında Prometheus formatında metrikler dönmeli."""
    
        # Henüz /metrics rotası yazılmadığı için 404 dönecektir (RED).
        response = client.get("/metrics")
    
        # Başarılı olduğunda 200 dönmeli
>       assert response.status_code == 200
E       assert 401 == 200
E        +  where 401 = <Response [401 Unauthorized]>.status_code

dispatcher/tests/test_metrics.py:21: AssertionError
=========================== short test summary info ============================
FAILED dispatcher/tests/test_rate_limit.py::TestRateLimiter::test_rate_limit_exceeded_returns_429 - assert 200 == 429
FAILED dispatcher/tests/test_metrics.py::TestMetrics::test_metrics_endpoint_returns_prometheus_format - assert 401 == 200
============================== 2 failed in 0.49s ===============================
